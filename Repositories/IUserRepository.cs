/* 
 * An Interface is like a 'Contract'. 
 * It tells the app what methods MUST be in any class that implements this repository.
 * It's useful for dependency injection and testing.
 */

namespace eproject_backend.Repositories
{
    public interface IUserRepository
    {
        // Currently empty, but can define what methods a UserRepository should have.
    }
}
