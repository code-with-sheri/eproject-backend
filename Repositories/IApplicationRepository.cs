/* 
 * This Interface defines the 'contract' for the ApplicationRepository.
 */

namespace eproject_backend.Repositories
{
    public interface IApplicationRepository
    {
        // Currently empty, but would define what a class that manages Applications MUST do.
    }
}
